"use strict";

document.writeln( "Hello, world!" );

var NaN = "hi";

isNaN( NaN );
document.writeln( isNaN( NaN ) );

var empty_object = {};
var stooge = {
    "first-name": "Jerome",
    "last-name": "Howard"
}
