"use strict";

document.writeln( "Hello, world!" );

var NaN = "hi";

isNaN( NaN );
document.writeln( isNaN( NaN ) );

var emptyObject = {};
var stooge = {
    "first-name": "Jerome",
    lastName: "Howard"
}
